{
  "name": "claude-api-proxy",
  "version": "1.0.0",
  "description": "Cloud Run proxy for Anthropic Claude API (API key stays in Secret Manager)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google-cloud/secret-manager": "^5.6.0"
  }
}
