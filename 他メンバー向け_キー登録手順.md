# 他メンバー向け：Claude APIキー登録手順（具体）

この手順どおり進めれば、自分のPCから GCP Secret Manager にキーを登録できます。

---

## 事前に必要なもの

- GitHub `T8-Studio` へのアクセス
- GCP プロジェクト `t8studio-infra-jp` へのアクセス（すでに確認済みならOK）
- Anthropic の APIキー（`sk-ant-...`）
- 自分のPCに入っているとよいソフト
  - Git
  - Node.js
  - Google Cloud SDK（`gcloud`）

インストールリンク（未導入の場合）:

- Node.js: https://nodejs.org/
- Google Cloud SDK: https://cloud.google.com/sdk/docs/install

---

## 手順A. 初回セットアップ（1人1回）

### 1. リポジトリをPCに取得する

PowerShell またはコマンドプロンプトで:

```powershell
cd %USERPROFILE%\Documents
git clone https://github.com/T8-Studio/claude-api-proxy.git
cd claude-api-proxy
```

取得先の例:

`C:\Users\（自分のユーザー名）\Documents\claude-api-proxy`

### 2. GCPにログインする

```powershell
gcloud auth login
gcloud config set project t8studio-infra-jp
```

ブラウザが開くので、GCPにアクセスできる Google アカウントでログインする。

確認:

```powershell
gcloud config get-value project
```

→ `t8studio-infra-jp` と出ればOK。

### 3. 管理ツールを起動できるか確認する

エクスプローラーで次のファイルをダブルクリック:

`claude-api-proxy\start-admin.cmd`

通常はブラウザで http://127.0.0.1:8787/ が開く。

**別の画面（Genspark など）が出る場合**  
8787番ポートが他アプリに使われています。そのときは PowerShell で:

```powershell
cd %USERPROFILE%\Documents\claude-api-proxy
set ADMIN_PORT=8788
node admin\server.js
```

その後ブラウザで http://127.0.0.1:8788/ を開く。

画面タイトルが **Claude Key Admin** / **T8 STUDIO** なら正解です。

---

## 手順B. キーを登録する（毎回）

### 1. Tool ID を決める

ルール:

- 小文字の英数字とハイフンのみ
- ツール名／リポジトリ名に合わせる

例:

| ツール | Tool ID |
|---|---|
| altplus | `altplus` |
| デザイン探索 | `design-search` |
| 書面掲示 | `syomen` |

### 2. 管理画面で入力する

1. Tool ID を入れる（例: `my-tool`）
2. Anthropic API Key を貼る（`sk-ant-...`）
3. **「登録後に Git push して Cloud Run へ自動デプロイ」にチェックを入れる**
4. **「登録する」を押す**

### 3. 成功を確認する

右側の実行ログに次のような表示が出れば成功です。

- `成功`
- `toolId: ...`
- `secret: claude-api-key-...`
- `header: X-Tool-Id: ...`

左側「登録済みツール」に自分の Tool ID が増えていればOK。

GCPコンソールでも確認できます。

- Secret Manager: https://console.cloud.google.com/security/secret-manager?project=t8studio-infra-jp  
- 名前: `claude-api-key-（Tool ID）`

---

## 手順C. ツールのコード側（キーは書かない）

キー登録後、ツール本体では次だけ使います。

- URL: `https://claude-proxy-3fwuq2jhrq-an.a.run.app`
- ヘッダー: `X-Tool-Id: （登録したTool ID）`

### JavaScript 例

```js
const CLAUDE_PROXY_URL = 'https://claude-proxy-3fwuq2jhrq-an.a.run.app';
const CLAUDE_TOOL_ID = 'my-tool';

await fetch(CLAUDE_PROXY_URL, {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'X-Tool-Id': CLAUDE_TOOL_ID
  },
  body: JSON.stringify({
    model: 'claude-haiku-4-5-20251001',
    max_tokens: 2048,
    messages: [{ role: 'user', content: '...' }]
  })
});
```

### PHP 例

```php
define('CLAUDE_PROXY_URL', 'https://claude-proxy-3fwuq2jhrq-an.a.run.app');
define('CLAUDE_TOOL_ID', 'my-tool');
// curl先は CLAUDE_PROXY_URL
// ヘッダーに X-Tool-Id を付ける
// x-api-key は付けない
```

**禁止:** `sk-ant-...` をソースに書いて GitHub へ push すること。

---

## うまくいかないとき

| 症状 | やること |
|---|---|
| 登録時に認証エラー | `gcloud auth login` をやり直す |
| 権限エラー | GCP `t8studio-infra-jp` に入れているか確認。入れない場合は管理者へ依頼 |
| 8787で別ツールが開く | `ADMIN_PORT=8788` で起動（上記） |
| Claude Key Admin が開かない | Node.js が入っているか確認。`node -v` でバージョンが出ること |
| 登録は成功したがツールが動かない | Tool ID のスペルがコードと完全一致しているか確認 |

---

## 最短チェックリスト

- [ ] `git clone` した
- [ ] `gcloud auth login` した
- [ ] プロジェクトが `t8studio-infra-jp`
- [ ] Claude Key Admin が開いた
- [ ] Tool ID + キーを登録して「成功」が出た
- [ ] Secret Manager に `claude-api-key-...` がある
- [ ] ツールコードにキーを書いていない

---

関連リポジトリ: https://github.com/T8-Studio/claude-api-proxy  
詳細手順書: 同リポジトリの `手順書.md`
