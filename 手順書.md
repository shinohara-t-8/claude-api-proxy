# Claude APIキー管理 手順書

## 1. 目的

各ツールのコードに Anthropic Claude APIキーを直書きせず、GCP上で安全に保管・利用する。

- キーの保管先: GCP Secret Manager
- 呼び出し窓口: Cloud Run プロキシ（`claude-api-proxy`）
- キー登録UI: 各自PCの Claude Key Admin（ローカルツール）

---

## 2. 全体像

```
[開発者のPC]
  Claude Key Admin でキー登録
       ↓
[GCP Secret Manager]  claude-api-key-<tool-id>
       ↓
[Cloud Run プロキシ]  X-Tool-Id でキーを選択して Claude API を呼ぶ
       ↑
[各ツール]  キーは持たず、プロキシURL + X-Tool-Id だけ送る
```

**GitHub には APIキーを置かない。**

---

## 3. 役割分担

| 役割 | やること |
|---|---|
| ツール利用者 | 完成したツールを使うだけ（Admin / GCP 不要） |
| ツール開発者 | Admin でキー登録 → プロキシ経由で実装 → GitHub へ push |

この手順書の主な対象は **ツール開発者** です。

---

## 4. 共通情報

| 項目 | 値 |
|---|---|
| GCPプロジェクト | `t8studio-infra-jp` |
| プロキシリポジトリ | https://github.com/T8-Studio/claude-api-proxy |
| プロキシURL | `https://claude-proxy-3fwuq2jhrq-an.a.run.app` |
| シークレット名規則 | `claude-api-key-<tool-id>` |
| 必須ヘッダー | `X-Tool-Id: <tool-id>` |
| 管理ツール起動 | `claude-api-proxy\start-admin.cmd` |
| 管理画面URL | http://127.0.0.1:8787 （localhostのみ） |

### 登録済みツール例

| Tool ID | Secret名 |
|---|---|
| `altplus` | `claude-api-key-altplus` |
| `design-search` | `claude-api-key-design-search` |

---

## 5. 開発者の初回セットアップ（1人あたり1回）

### 5.1 リポジトリを取得

```powershell
git clone https://github.com/T8-Studio/claude-api-proxy.git
cd claude-api-proxy
```

### 5.2 必要なソフト

- Git
- Node.js
- Google Cloud SDK（`gcloud`）

### 5.3 GCPログイン

```powershell
gcloud auth login
gcloud config set project t8studio-infra-jp
```

Secret Manager を操作できる権限が必要です（例: 組織グループ経由の権限）。

### 5.4 管理ツール起動確認

エクスプローラーで `start-admin.cmd` をダブルクリックし、  
http://127.0.0.1:8787 が開けばOK。

> Cursor上のリンクから `.cmd` を開くと失敗することがあるため、エクスプローラーから起動すること。

---

## 6. 推奨開発フロー（ローカル段階からキーを移す）

**直書きしてあとで消すより、最初からプロキシ経由にする。**

```
① Tool ID を決める
② Claude Key Admin でキー登録
③ 最初からプロキシURL + X-Tool-Id で実装
④ ローカルで動作確認（キーがコードに無いこと）
⑤ GitHub に push
```

---

## 7. Claude Key Admin の使い方

### 7.1 起動

1. `claude-api-proxy` フォルダを開く
2. `start-admin.cmd` をダブルクリック
3. ブラウザで管理画面が開く

### 7.2 新規キー登録

1. **Tool ID** を入力（例: `auditor`）
   - 小文字英数字とハイフンのみ
   - リポジトリ名に合わせると分かりやすい
2. **Anthropic API Key** を貼り付け（`sk-ant-...`）
3. **「登録後に Git push して Cloud Run へ自動デプロイ」にチェック**（推奨）
4. **「登録する」をクリック**

### 7.3 成功時に確認すること

ログに次が出ること。

- `成功`
- `toolId: ...`
- `secret: claude-api-key-...`
- `header: X-Tool-Id: ...`
- `url: https://claude-proxy-...`

「登録済みツール」一覧に追加されていれば完了。

### 7.4 Admin が自動で行うこと

1. Secret Manager に `claude-api-key-<tool-id>` を作成（既存なら新バージョン追加）
2. Cloud Run 実行用サービスアカウントへ `secretAccessor` 付与
3. プロキシの Tool ID マップを更新
4. （チェックON時）GitHub へ push → Actions で Cloud Run デプロイ

---

## 8. ツール側の実装方法

コードに APIキーは書かない。次だけ持つ。

### 8.1 JavaScript / HTML

```js
const CLAUDE_PROXY_URL = 'https://claude-proxy-3fwuq2jhrq-an.a.run.app';
const CLAUDE_TOOL_ID = 'auditor'; // Admin で登録した Tool ID

const res = await fetch(CLAUDE_PROXY_URL, {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'X-Tool-Id': CLAUDE_TOOL_ID
  },
  body: JSON.stringify({
    model: 'claude-haiku-4-5-20251001',
    max_tokens: 2048,
    messages: [{ role: 'user', content: '...' }]
  })
});
```

### 8.2 PHP

```php
define('CLAUDE_PROXY_URL', 'https://claude-proxy-3fwuq2jhrq-an.a.run.app');
define('CLAUDE_TOOL_ID', 'auditor');

// curl 送信先: CLAUDE_PROXY_URL
// ヘッダー:
//   Content-Type: application/json
//   X-Tool-Id: auditor
// x-api-key は付けない
```

### 8.3 レスポンス

Anthropic Messages API と同じ形式が返る。  
（`content[0].text` などを従来どおり利用可能）

---

## 9. 動作確認チェックリスト

ローカルで一度動かしたあと、次を確認する。

- [ ] AI機能が普通に動く
- [ ] コード内に `sk-ant-` が無い
- [ ] コード内に `x-api-key` が無い
- [ ] 送信先が `api.anthropic.com` ではなくプロキシURL
- [ ] リクエストヘッダーに `X-Tool-Id` がある
- [ ] ブラウザの開発者ツール（F12）でもキーが見えない

問題なければ GitHub へ push する。

---

## 10. キーの更新（ローテーション）

1. Admin を起動
2. **同じ Tool ID** を入力
3. 新しい APIキーを登録
4. ツール側コードの変更は不要

Anthropic 側で古いキーを無効化する。

---

## 11. やってはいけないこと

- APIキーをソースコードに直書きしたまま GitHub へ push する
- Tool ID を登録せずにプロキシを呼ぶ
- 他人の Tool ID を勝手に使い回す（課金・制限の切り分けが難しくなる）
- 管理画面URLをインターネットに公開する（localhost専用）

---

## 12. よくある問題

| 症状 | 確認・対処 |
|---|---|
| Admin が開かない | エクスプローラーから `start-admin.cmd` を起動する |
| 登録時に認証エラー | `gcloud auth login` を再実行 |
| 登録時に権限エラー | プロジェクト `t8studio-infra-jp` の権限を確認 |
| ツールから HTTP 400 | `X-Tool-Id` が登録済み ID と一致しているか確認 |
| ツールから HTTP 500 | デプロイ完了前の可能性。少し待って再試行。Secret / IAM も確認 |
| push しても反映されない | GitHub Actions（`claude-api-proxy`）の成否を確認 |

---

## 13. CLI で登録する場合（任意）

Admin UI の代わりにスクリプトでも可。

```powershell
cd C:\Users\user\Documents\claude-api-proxy
.\scripts\add-tool.ps1 -ToolId "auditor" -ApiKey "sk-ant-..." -Push
```

---

## 14. 関連リンク

- プロキシリポジトリ: https://github.com/T8-Studio/claude-api-proxy
- Secret Manager: https://console.cloud.google.com/security/secret-manager?project=t8studio-infra-jp
- Cloud Run: https://console.cloud.google.com/run?project=t8studio-infra-jp

---

## 改訂履歴

| 日付 | 内容 |
|---|---|
| 2026-07-31 | 初版。個人インストール型 Admin + ローカル段階からのキー移設フローを整理 |
